# MongoDB Atlas Connection URI
# Replace with your MongoDB Atlas connection string or keep local for offline testing
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/healthy_future?retryWrites=true&w=majority

# JWT Secret for Session Authentication
JWT_SECRET=healthy_future_jwt_secure_key_2026_vercel

# Server Port (Only used in local non-serverless mode)
PORT=5000
